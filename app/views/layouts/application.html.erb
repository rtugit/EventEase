<!DOCTYPE html>
<html>
  <head>
    <title>EventEase - Focus on the event. Not the coordination.</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Find and create events near you. Connect with people who share your interests.">

    <%# Open Graph Meta Tags %>
    <meta property="og:title" content="EventEase - Discover and Join Amazing Events">
    <meta property="og:description" content="Find and create events near you. Connect with people who share your interests.">
    <meta property="og:image" content="https://www.eventease.live/og-image.png">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:url" content="https://www.eventease.live">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="EventEase">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@200..800&display=swap" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <meta name="theme-color" content="#1B1E2A">
  </head>

  <body>
    <% hide_layout = (controller_name == 'sessions') ||
                    (controller_name == 'registrations' && action_name == 'new') ||
                    (controller_name == 'passwords') ||
                    (controller_name == 'pages' && action_name == 'landing') %>

    <% unless hide_layout %>
      <%= render "shared/navbar" %>
    <% end %>

    <%= render "shared/flashes" %>

    <main>
      <%= yield %>
    </main>

    <% unless hide_layout %>
      <%= render "shared/footer" %>
    <% end %>

    <%# AI Chatbot %>
    <%= render "layouts/chat_button" %>
    <%= render "layouts/chat_modal" %>
  </body>
</html>
